---
title: '987.二叉树的垂序遍历'
date: 2026-05-17
tags: [算法, LeetCode, 二叉树, DFS]
---

### 题目描述:
给你二叉树的根结点 `root` ，请你设计算法计算二叉树的 **垂序遍历** 序列。

对位于 `(row, col)` 的每个结点而言，其左右子结点分别位于 `(row + 1, col - 1)` 和 `(row + 1, col + 1)` 。树的根结点位于 `(0, 0)` 。

二叉树的 **垂序遍历** 从最左边的列开始直到最右边的列结束，按列索引每一列上的所有结点，形成一个按出现位置从上到下排序的有序列表。如果同行同列上有多个结点，则按结点的值从小到大进行排序。

返回二叉树的 **垂序遍历** 序列。

**示例 1：**

![](https://assets.leetcode.com/uploads/2021/01/29/vtree1.jpg)

**输入:** root = `[3,9,20,null,null,15,7]`
**输出:**`[[9],[3,15],[20],[7]]`
**解释：**
列 -1 ：只有结点 9 在此列中。
列  0 ：只有结点 3 和 15 在此列中，按从上到下顺序。
列  1 ：只有结点 20 在此列中。
列  2 ：只有结点 7 在此列中。

**示例 2：**

![](https://assets.leetcode.com/uploads/2021/01/29/vtree2.jpg)

**输入：** root = `[1,2,3,4,5,6,7]`
**输出：** `[[4],[2],[1,5,6],[3],[7]]`
**解释：**
列 -2 ：只有结点 4 在此列中。
列 -1 ：只有结点 2 在此列中。
列  0 ：结点 1 、5 和 6 都在此列中。
          1 在上面，所以它出现在前面。
          5 和 6 位置都是 (2, 0) ，所以按值从小到大排序，5 在 6 的前面。
列  1 ：只有结点 3 在此列中。
列  2 ：只有结点 7 在此列中。

**示例 3：**

![](https://assets.leetcode.com/uploads/2021/01/29/vtree3.jpg)

**输入：** root = `[1,2,3,4,6,5,7]`
**输出：** `[4],[2],[1,5,6],[3],[7]`
**解释：**
这个示例实际上与示例 2 完全相同，只是结点 5 和 6 在树中的位置发生了交换。
因为 5 和 6 的位置仍然相同，所以答案保持不变，仍然按值从小到大排序。

**提示：**

- 树中结点数目总数在范围 `[1, 1000]` 内
- `0 <= Node.val <= 1000`

---
### 解题思路:

在构造二叉树的时候为每个结点赋予相应的坐标:(height , row). 其中,height为该结点所在位置的树高, row为在同一层树高的左右偏移,遵循左正右负的规则.
- 根结点的坐标为(0,0)
- 树的结点从根结点开始展开,如果是左孩子,那么height+1, row-1; 如果是右孩子则height+1, row+1. 坐标的计算方法是根据父结点的内容进行如上的修改.
- **遍历方式**​：深度优先搜索(DFS)遍历整棵树，记录每个节点的坐标和值
- **排序规则**:   首先按列号从小到大排序, 同一列内先按行号排序, 行号相同则按节点值排序

需要考虑,在结点的定义中并没有对坐标这一项时,如何使子结点能够调用父结点坐标的内容并在其基础上进行修改.-->用一个哈希表（或者有序字典）来记录这些数据。哈希表的 key 是 col，哈希表的 value 是一个列表，列表中保存 (row,val) 二元组


---
### 解题过程

结点声明
```Java
public class TreeNode {  
    int val;  
    TreeNode left;  
    TreeNode right;  
    TreeNode() {}  
    TreeNode(int val) { this.val = val; }  
    TreeNode(int val, TreeNode left, TreeNode right) {  
        this.val = val;  
        this.left = left;  
        this.right = right;  
    }  
}

```

---

垂序遍历方法实现
#### 法1:TreeMap存储col, 二元组存储row 和 val
```Java
public List<List<Integer>> verticalTraversal(TreeNode root) {  
    // 使用TreeMap自动按列号(col)排序  
    Map<Integer, List<int[]>> groups = new TreeMap<>();  
    // 深度优先遍历，记录每个节点的位置和值  
    dfs(root, 0, 0, groups);  
  
    List<List<Integer>> res = new ArrayList<>();  
    // 处理每一列的数据  
    for(List<int[]> g : groups.values()){  
        // 对同一列内的节点排序：先按行号，行号相同按值  
        g.sort((a,b)->a[0]!=b[0]?a[0]-b[0]:a[1]-b[1]);  
  
        // 将排序后的值提取出来  
        List<Integer> list = new ArrayList<>();  
        for(int[] p : g){  
            list.add(p[1]);  
        }  
        res.add(list);  
    }  
    return res;  
}  
  
/**  
 * 深度优先遍历实现  
 * @param node 当前节点  
 * @param row 当前行号  
 * @param col 当前列号  
 * @param groups 存储分组结果的Map  
 */public void dfs(TreeNode node, int row, int col, Map<Integer, List<int[]>> groups){  
    if (node == null){  
        return;  
    }  
    // 将当前节点加入对应列的分组中  
    // computeIfAbsent: 如果列号不存在则创建新列表  
    groups.computeIfAbsent(col, k -> new ArrayList<>())  
            .add(new int[] {row, node.val});  
  
    // 递归处理左子树(行号+1，列号-1)  
    dfs(node.left, row + 1, col - 1, groups);  
    // 递归处理右子树(行号+1，列号+1)  
    dfs(node.right, row + 1, col + 1, groups);  
}
```

#### 法2:直接使用三元组(row , col , val)存储
```Java
class Solution {
    List<Triple> nodes = new ArrayList<>();

    public List<List<Integer>> verticalTraversal(TreeNode root) {
        traverse(root, 0, 0);
        nodes.sort((Triple a, Triple b) -> {
            if (a.col == b.col && a.row == b.row) {
                return a.node.val - b.node.val;
            }
            if (a.col == b.col) {
                return a.row - b.row;
            }
            return a.col - b.col;
        });

        LinkedList<List<Integer>> res = new LinkedList<>();
        int preCol = Integer.MIN_VALUE;
        for (Triple cur : nodes) {
            if (cur.col != preCol) {
                res.addLast(new LinkedList<>());
                preCol = cur.col;
            }
            res.getLast().add(cur.node.val);
        }

        return res;
    }

    void traverse(TreeNode root, int row, int col) {
        if (root == null) {
            return;
        }
        nodes.add(new Triple(root, row, col));
        traverse(root.left, row + 1, col - 1);
        traverse(root.right, row + 1, col + 1);
    }

    class Triple {
        int row;
        int col;
        TreeNode node;

        public Triple(TreeNode node, int row, int col) {
            this.node = node;
            this.row = row;
            this.col = col;
        }
    }
}
```




---
### 总结

#### 复杂度分析

- ​**时间复杂度**​：O(NlogN)
    - DFS遍历：O(N)
    - 排序：最坏情况下所有节点都在同一列，需要O(NlogN)排序
- ​**空间复杂度**​：O(N)
    - 存储所有节点的信息

#### 关键点总结
1. ​**坐标系统**​：明确节点坐标的计算规则
2. ​**分组存储**​：使用TreeMap自动按列号排序
3. ​**排序规则**​：同一列内先按行号再按值排序
4. ​**遍历方式**​：DFS适合记录每个节点的位置信息
